import type { ComponentDoc } from './types';
import Callout2Playground from '../../_playgrounds/callout2/Callout2Playground.svelte';

export const callout2Doc: ComponentDoc = {
  name: 'Callout2',
  description: 'Popover2 + callout class removes the need for a separate callout component.'
};
